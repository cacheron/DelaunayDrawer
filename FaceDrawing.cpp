
#include <iostream>

int nothing() {
	// take video id as arg 1
	// take video range as arg 2 and 3 (frame 1 --> 50)
	// get all filenames that match <ith frame>.png
	// load the <ith> file
		// get the facial data from the database that has the same video id and frame id
		// parse the input into a vector
			// for every point in the vector,
			// draw a dot
	return 0;
	system("pause");
}